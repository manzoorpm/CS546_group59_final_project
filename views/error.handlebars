<div class="d-flex align-items-center justify-content-center vh-100">
    <div class="text-center">
        <h1 class="display-1 fw-bold">Error:</h1>
        <p class="fs-3"> <span class="text-danger">Oops!</span>{{error}}</p>

        <p class="lead">
            Resopense contains some errors
        </p>
        {{#if capacityError}}
        <a href="/restaurant/{{restaurantId}}" class="btn btn-primary">Check Another Slot</a>
        {{else}}
        <a href="/" class="btn btn-primary">Go Home</a>
        {{/if}}
    </div>
</div>